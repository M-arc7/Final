import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';

const root = join(process.cwd(), 'packages/core');
const write = (path, body) => { mkdirSync(dirname(path), { recursive: true }); writeFileSync(path, body.trimStart()); };
const file = (path) => join(root, path);

// Account: canonical identity, deliberately free of role or organisation concerns.
write(file('identity/account/account.types.ts'), `import type { Brand, Metadata, Timestamped } from '../../shared/primitives';
export type AccountId = Brand<string, 'AccountId'>;
export type AccountStatus = 'pending' | 'active' | 'suspended' | 'locked' | 'deactivated';
export type AccountIdentity = Readonly<{ provider: string; providerSubject: string }>;
export type Account = Readonly<{ id: AccountId; identity: AccountIdentity; status: AccountStatus; metadata: Metadata } & Timestamped>;
export type AccountCreateInput = Readonly<{ id?: AccountId; identity: AccountIdentity; metadata?: Metadata }>;
export type AccountUpdateInput = Readonly<{ metadata?: Metadata }>;
export type AccountSummary = Readonly<Pick<Account, 'id' | 'status' | 'createdAt'>>;
`);
write(file('identity/account/account.entity.ts'), `import { invariant } from '../../shared/errors';
import { newId } from '../../shared/primitives';
import type { Account, AccountCreateInput, AccountStatus } from './account.types';
const transitions: Readonly<Record<AccountStatus, readonly AccountStatus[]>> = { pending: ['active', 'deactivated'], active: ['suspended', 'locked', 'deactivated'], suspended: ['active', 'deactivated'], locked: ['active', 'deactivated'], deactivated: [] };
/** Pure one-account identity model. Roles and organisation membership are not account fields. */
export const createAccount = (input: AccountCreateInput, now = new Date()): Account => ({ id: input.id ?? newId<'AccountId'>(), identity: input.identity, status: 'pending', metadata: input.metadata ?? {}, createdAt: now, updatedAt: now });
export const transitionAccountStatus = (account: Account, next: AccountStatus, now = new Date()): Account => { invariant(transitions[account.status].includes(next), 'account.invalid_status_transition', 'The requested account lifecycle transition is not allowed.', { from: account.status, to: next }); return { ...account, status: next, updatedAt: now }; };
`);
write(file('identity/account/account.schema.ts'), `import { z } from 'zod';
export const accountIdSchema = z.string().uuid();
export const accountStatusSchema = z.enum(['pending', 'active', 'suspended', 'locked', 'deactivated']);
export const accountIdentitySchema = z.object({ provider: z.string().min(1).max(80), providerSubject: z.string().min(1).max(255) }).strict();
export const createAccountSchema = z.object({ id: accountIdSchema.optional(), identity: accountIdentitySchema, metadata: z.record(z.unknown()).default({}) }).strict();
export const updateAccountSchema = z.object({ metadata: z.record(z.unknown()).optional() }).strict();
export const accountStatusTransitionSchema = z.object({ status: accountStatusSchema }).strict();
`);
write(file('identity/account/account.repository.ts'), `import type { Account, AccountId, AccountIdentity, AccountStatus } from './account.types';
/** Persistence port only; it contains no authorization decisions or provider SDK calls. */
export interface AccountRepository { create(account: Account): Promise<Account>; findById(id: AccountId): Promise<Account | null>; findByIdentity(identity: AccountIdentity): Promise<Account | null>; update(account: Account): Promise<Account>; existsByIdentity(identity: AccountIdentity): Promise<boolean>; changeStatus(id: AccountId, status: AccountStatus): Promise<Account>; }
`);
write(file('identity/account/account.service.ts'), `import { CoreError } from '../../shared/errors';
import { createAccount, transitionAccountStatus } from './account.entity';
import type { AccountRepository } from './account.repository';
import type { Account, AccountCreateInput, AccountId, AccountStatus } from './account.types';
/** Account lifecycle use cases. This service has no organisation role or permission logic. */
export class AccountService { constructor(private readonly repository: AccountRepository, private readonly now: () => Date = () => new Date()) {} async create(input: AccountCreateInput): Promise<Account> { if (await this.repository.existsByIdentity(input.identity)) throw new CoreError('account.identity_exists', 'An account cannot be created for this identity.', {}); return this.repository.create(createAccount(input, this.now())); } async get(id: AccountId) { return this.repository.findById(id); } async activate(id: AccountId) { return this.changeStatus(id, 'active'); } async deactivate(id: AccountId) { return this.changeStatus(id, 'deactivated'); } async changeStatus(id: AccountId, status: AccountStatus): Promise<Account> { const account = await this.repository.findById(id); if (!account) throw new CoreError('account.not_found', 'The requested account does not exist.', {}); return this.repository.update(transitionAccountStatus(account, status, this.now())); } }
`);
write(file('identity/account/index.ts'), `export * from './account.types';
export * from './account.entity';
export * from './account.schema';
export * from './account.repository';
export * from './account.service';
`);

// Authentication proves identity. It has no roles, permissions, resource or organisation concepts.
write(file('identity/authentication/authentication.types.ts'), `import type { AccountId } from '../account';
import type { Brand } from '../../shared/primitives';
export type AuthenticationMethod = 'password' | 'otp' | 'oauth';
export type AuthenticationProvider = 'email' | 'phone' | 'google' | 'apple' | (string & {});
export type AuthenticationSessionToken = Brand<string, 'AuthenticationSessionToken'>;
export type SignUpInput = Readonly<{ provider: AuthenticationProvider; identifier: string; secret?: string }>;
export type SignInInput = Readonly<{ provider: AuthenticationProvider; identifier: string; secret?: string; verificationCode?: string; deviceId?: string }>;
export type SignOutInput = Readonly<{ sessionId: string }>;
export type PasswordResetInput = Readonly<{ identifier: string; resetToken?: string; newSecret?: string }>;
export type AuthenticationResult = Readonly<{ accountId: AccountId; sessionId?: string; verificationRequired: boolean; mfaRequired: boolean }>;
export type AuthenticationContext = Readonly<{ accountId: AccountId; sessionId: string; authenticatedAt: Date }>;
`);
write(file('identity/authentication/authentication.schema.ts'), `import { z } from 'zod';
const identifier = z.string().trim().min(3).max(320);
const secret = z.string().min(12).max(1024);
export const signUpSchema = z.object({ provider: z.string().min(1).max(80), identifier, secret: secret.optional() }).strict();
export const signInSchema = z.object({ provider: z.string().min(1).max(80), identifier, secret: secret.optional(), verificationCode: z.string().min(4).max(32).optional(), deviceId: z.string().uuid().optional() }).strict();
export const signOutSchema = z.object({ sessionId: z.string().uuid() }).strict();
export const passwordResetSchema = z.object({ identifier, resetToken: z.string().min(16).max(1024).optional(), newSecret: secret.optional() }).strict();
`);
write(file('identity/authentication/authentication.providers.ts'), `import type { AuthenticationProvider, SignInInput, SignUpInput } from './authentication.types';
/** Provider port only. Provider implementations live in infrastructure and must not return plaintext secrets. */
export type ProviderIdentity = Readonly<{ provider: AuthenticationProvider; providerSubject: string; identifier: string; verified: boolean }>;
export interface IdentityProvider { readonly name: AuthenticationProvider; signUp(input: SignUpInput): Promise<ProviderIdentity>; signIn(input: SignInInput): Promise<ProviderIdentity>; signOut?(providerSubject: string): Promise<void>; requestPasswordReset?(identifier: string): Promise<void>; resetPassword?(input: Readonly<{ token: string; newSecret: string }>): Promise<void>; }
`);
write(file('identity/authentication/authentication.repository.ts'), `import type { AccountId } from '../account';
import type { AuthenticationProvider, ProviderIdentity } from './authentication.providers';
/** Persistence port for metadata/events only. Passwords and raw provider secrets are never application records. */
export interface AuthenticationRepository { recordEvent(event: Readonly<{ accountId?: AccountId; provider: AuthenticationProvider; type: 'sign_up' | 'sign_in' | 'sign_out' | 'reset_requested' | 'reset_completed'; occurredAt: Date }>): Promise<void>; findProviderIdentity(provider: AuthenticationProvider, providerSubject: string): Promise<ProviderIdentity | null>; }
`);
write(file('identity/authentication/authentication.errors.ts'), `import { CoreError } from '../../shared/errors';
export type AuthenticationErrorCode = 'invalid_credentials' | 'account_not_found' | 'account_locked' | 'account_suspended' | 'verification_required' | 'session_expired' | 'unsupported_provider' | 'rate_limited';
export class AuthenticationError extends CoreError { constructor(code: AuthenticationErrorCode, context: Readonly<Record<string, unknown>> = {}) { super(code, 'Authentication could not be completed.', context); } }
`);
write(file('identity/authentication/authentication.service.ts'), `import type { AccountService } from '../account';
import type { SessionService } from '../sessions';
import type { VerificationService } from '../verification';
import { AuthenticationError } from './authentication.errors';
import type { AuthenticationRepository } from './authentication.repository';
import type { IdentityProvider } from './authentication.providers';
import type { AuthenticationResult, PasswordResetInput, SignInInput, SignUpInput } from './authentication.types';
/** Identity orchestration only: account → verification → session. Authorization is intentionally absent. */
export class AuthenticationService { constructor(private readonly accounts: AccountService, private readonly sessions: SessionService, private readonly verification: VerificationService, private readonly events: AuthenticationRepository, private readonly providers: ReadonlyMap<string, IdentityProvider>, private readonly now: () => Date = () => new Date()) {} private provider(name: string): IdentityProvider { const provider = this.providers.get(name); if (!provider) throw new AuthenticationError('unsupported_provider'); return provider; } async signUp(input: SignUpInput): Promise<AuthenticationResult> { const identity = await this.provider(input.provider).signUp(input); const account = await this.accounts.create({ identity: { provider: identity.provider, providerSubject: identity.providerSubject } }); await this.events.recordEvent({ accountId: account.id, provider: input.provider, type: 'sign_up', occurredAt: this.now() }); await this.verification.create({ accountId: account.id, purpose: 'identity', channel: input.provider, expiresAt: new Date(this.now().getTime() + 15 * 60_000) }); return { accountId: account.id, verificationRequired: !identity.verified, mfaRequired: false }; } async signIn(input: SignInInput): Promise<AuthenticationResult> { const identity = await this.provider(input.provider).signIn(input); const account = await this.accounts.getByIdentity({ provider: identity.provider, providerSubject: identity.providerSubject }); if (!account) throw new AuthenticationError('invalid_credentials'); if (account.status === 'locked') throw new AuthenticationError('account_locked'); if (account.status === 'suspended' || account.status === 'deactivated') throw new AuthenticationError('account_suspended'); if (!identity.verified) return { accountId: account.id, verificationRequired: true, mfaRequired: false }; const session = await this.sessions.create({ accountId: account.id, deviceId: input.deviceId, expiresAt: new Date(this.now().getTime() + 60 * 60_000) }); await this.events.recordEvent({ accountId: account.id, provider: input.provider, type: 'sign_in', occurredAt: this.now() }); return { accountId: account.id, sessionId: session.id, verificationRequired: false, mfaRequired: false }; } async signOut(input: Readonly<{ sessionId: string }>): Promise<void> { await this.sessions.revoke(input.sessionId); } async requestPasswordReset(input: Pick<PasswordResetInput, 'identifier'>): Promise<void> { for (const provider of this.providers.values()) await provider.requestPasswordReset?.(input.identifier); } async resetPassword(input: Required<PasswordResetInput>): Promise<void> { for (const provider of this.providers.values()) if (provider.resetPassword) await provider.resetPassword({ token: input.resetToken, newSecret: input.newSecret }); } }
`);
write(file('identity/authentication/index.ts'), `export * from './authentication.types';
export * from './authentication.schema';
export * from './authentication.providers';
export * from './authentication.repository';
export * from './authentication.errors';
export * from './authentication.service';
`);

write(file('identity/sessions/session.types.ts'), `import type { AccountId } from '../account';
import type { Brand, Metadata, Timestamped } from '../../shared/primitives';
export type SessionId = Brand<string, 'SessionId'>;
export type DeviceId = Brand<string, 'DeviceId'>;
export type SessionStatus = 'active' | 'expired' | 'revoked';
export type Session = Readonly<{ id: SessionId; accountId: AccountId; deviceId?: DeviceId; status: SessionStatus; lastActivityAt: Date; expiresAt: Date; revokedAt?: Date; ipMetadata?: Metadata } & Timestamped>;
export type SessionCreateInput = Readonly<{ id?: SessionId; accountId: AccountId; deviceId?: string; expiresAt: Date; ipMetadata?: Metadata }>;
export type SessionContext = Readonly<{ sessionId: SessionId; accountId: AccountId; expiresAt: Date }>;
export type SessionSummary = Readonly<Pick<Session, 'id' | 'deviceId' | 'status' | 'lastActivityAt' | 'expiresAt'>>;
`);
write(file('identity/sessions/session.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { Session, SessionCreateInput } from './session.types';
export const createSession = (input: SessionCreateInput, now = new Date()): Session => { invariant(input.expiresAt > now, 'session.invalid_expiry', 'A session expiry must be in the future.'); return { id: input.id ?? newId<'SessionId'>(), accountId: input.accountId, deviceId: input.deviceId as Session['deviceId'], status: 'active', lastActivityAt: now, expiresAt: input.expiresAt, ipMetadata: input.ipMetadata, createdAt: now, updatedAt: now }; };
export const revokeSession = (session: Session, now = new Date()): Session => ({ ...session, status: 'revoked', revokedAt: now, updatedAt: now });
export const isUsableSession = (session: Session, now = new Date()) => session.status === 'active' && session.expiresAt > now;
`);
write(file('identity/sessions/session.schema.ts'), `import { z } from 'zod'; export const sessionIdSchema = z.string().uuid(); export const createSessionSchema = z.object({ accountId: z.string().uuid(), deviceId: z.string().uuid().optional(), expiresAt: z.coerce.date(), ipMetadata: z.record(z.unknown()).optional() }).strict(); export const revokeSessionSchema = z.object({ sessionId: sessionIdSchema }).strict();`);
write(file('identity/sessions/session.repository.ts'), `import type { AccountId } from '../account'; import type { Session, SessionId } from './session.types'; export interface SessionRepository { create(session: Session): Promise<Session>; findById(id: string): Promise<Session | null>; update(session: Session): Promise<Session>; revokeAllForAccount(accountId: AccountId): Promise<void>; listActiveForAccount(accountId: AccountId): Promise<readonly Session[]>; }`);
write(file('identity/sessions/session.service.ts'), `import { CoreError } from '../../shared/errors'; import { createSession, isUsableSession, revokeSession } from './session.entity'; import type { SessionRepository } from './session.repository'; import type { Session, SessionCreateInput } from './session.types'; export class SessionService { constructor(private readonly repository: SessionRepository, private readonly now: () => Date = () => new Date()) {} async create(input: SessionCreateInput): Promise<Session> { return this.repository.create(createSession(input, this.now())); } async validate(id: string): Promise<Session> { const session = await this.repository.findById(id); if (!session || !isUsableSession(session, this.now())) throw new CoreError('session.invalid', 'The session is not active.', {}); return session; } async revoke(id: string): Promise<void> { const session = await this.repository.findById(id); if (session) await this.repository.update(revokeSession(session, this.now())); } async revokeAll(accountId: Session['accountId']) { await this.repository.revokeAllForAccount(accountId); } }`);
write(file('identity/sessions/index.ts'), `export * from './session.types'; export * from './session.entity'; export * from './session.schema'; export * from './session.repository'; export * from './session.service';`);

write(file('identity/devices/device.types.ts'), `import type { AccountId } from '../account'; import type { Brand, Metadata, Timestamped } from '../../shared/primitives'; export type DeviceId = Brand<string, 'DeviceId'>; export type DeviceTrust = 'untrusted' | 'trusted' | 'revoked'; export type Device = Readonly<{ id: DeviceId; accountId: AccountId; platform: 'ios' | 'android' | 'web' | 'other'; osVersion?: string; appVersion?: string; pushToken?: string; trust: DeviceTrust; metadata: Metadata } & Timestamped>; export type DeviceRegisterInput = Readonly<{ accountId: AccountId; platform: Device['platform']; osVersion?: string; appVersion?: string; pushToken?: string; metadata?: Metadata }>;`);
write(file('identity/devices/device.entity.ts'), `import { newId } from '../../shared/primitives'; import type { Device, DeviceRegisterInput } from './device.types'; export const registerDevice = (input: DeviceRegisterInput, now = new Date()): Device => ({ id: newId<'DeviceId'>(), accountId: input.accountId, platform: input.platform, osVersion: input.osVersion, appVersion: input.appVersion, pushToken: input.pushToken, trust: 'untrusted', metadata: input.metadata ?? {}, createdAt: now, updatedAt: now }); export const revokeDevice = (device: Device, now = new Date()): Device => ({ ...device, trust: 'revoked', pushToken: undefined, updatedAt: now });`);
write(file('identity/devices/device.schema.ts'), `import { z } from 'zod'; export const registerDeviceSchema = z.object({ accountId: z.string().uuid(), platform: z.enum(['ios', 'android', 'web', 'other']), osVersion: z.string().max(80).optional(), appVersion: z.string().max(80).optional(), pushToken: z.string().max(4096).optional(), metadata: z.record(z.unknown()).optional() }).strict();`);
write(file('identity/devices/device.repository.ts'), `import type { AccountId } from '../account'; import type { Device } from './device.types'; export interface DeviceRepository { create(device: Device): Promise<Device>; findById(id: string): Promise<Device | null>; update(device: Device): Promise<Device>; listForAccount(accountId: AccountId): Promise<readonly Device[]>; }`);
write(file('identity/devices/device.service.ts'), `import { registerDevice, revokeDevice } from './device.entity'; import type { DeviceRepository } from './device.repository'; import type { Device, DeviceRegisterInput } from './device.types'; export class DeviceService { constructor(private readonly repository: DeviceRepository, private readonly now: () => Date = () => new Date()) {} async register(input: DeviceRegisterInput) { return this.repository.create(registerDevice(input, this.now())); } async revoke(id: string): Promise<void> { const device = await this.repository.findById(id); if (device) await this.repository.update(revokeDevice(device, this.now())); } async updatePushToken(id: string, pushToken?: string): Promise<Device | null> { const device = await this.repository.findById(id); return device ? this.repository.update({ ...device, pushToken, updatedAt: this.now() }) : null; } }`);
write(file('identity/devices/index.ts'), `export * from './device.types'; export * from './device.entity'; export * from './device.schema'; export * from './device.repository'; export * from './device.service';`);

write(file('identity/verification/verification.types.ts'), `import type { AccountId } from '../account'; import type { Brand, Timestamped } from '../../shared/primitives'; export type VerificationId = Brand<string, 'VerificationId'>; export type VerificationPurpose = 'identity' | 'email' | 'phone' | 'password_reset' | 'mfa'; export type VerificationStatus = 'pending' | 'completed' | 'expired' | 'invalidated'; export type Verification = Readonly<{ id: VerificationId; accountId: AccountId; purpose: VerificationPurpose; channel: string; status: VerificationStatus; tokenHash?: string; expiresAt: Date; completedAt?: Date } & Timestamped>; export type VerificationCreateInput = Readonly<{ accountId: AccountId; purpose: VerificationPurpose; channel: string; expiresAt: Date }>; export type VerificationResult = Readonly<{ verified: boolean; status: VerificationStatus }>;`);
write(file('identity/verification/verification.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { Verification, VerificationCreateInput } from './verification.types'; export const createVerification = (input: VerificationCreateInput, tokenHash: string, now = new Date()): Verification => { invariant(input.expiresAt > now, 'verification.invalid_expiry', 'Verification expiry must be in the future.'); return { id: newId<'VerificationId'>(), accountId: input.accountId, purpose: input.purpose, channel: input.channel, status: 'pending', tokenHash, expiresAt: input.expiresAt, createdAt: now, updatedAt: now }; }; export const expireVerification = (verification: Verification, now = new Date()): Verification => ({ ...verification, status: 'expired', updatedAt: now }); export const completeVerification = (verification: Verification, now = new Date()): Verification => ({ ...verification, status: 'completed', tokenHash: undefined, completedAt: now, updatedAt: now });`);
write(file('identity/verification/verification.schema.ts'), `import { z } from 'zod'; export const createVerificationSchema = z.object({ accountId: z.string().uuid(), purpose: z.enum(['identity', 'email', 'phone', 'password_reset', 'mfa']), channel: z.string().min(1).max(80), expiresAt: z.coerce.date() }).strict(); export const verifyChallengeSchema = z.object({ verificationId: z.string().uuid(), token: z.string().min(4).max(1024) }).strict();`);
write(file('identity/verification/verification.repository.ts'), `import type { AccountId } from '../account'; import type { Verification } from './verification.types'; export interface VerificationRepository { create(verification: Verification): Promise<Verification>; findById(id: string): Promise<Verification | null>; update(verification: Verification): Promise<Verification>; invalidatePending(accountId: AccountId, purpose: Verification['purpose']): Promise<void>; }`);
write(file('identity/verification/verification.tokens.ts'), `/** Cryptographic port. Infrastructure must hash tokens and compare in constant time; raw tokens are never persisted. */ export interface VerificationTokenCodec { issue(): Promise<Readonly<{ token: string; hash: string }>>; verify(token: string, hash: string): Promise<boolean>; }`);
write(file('identity/verification/verification.service.ts'), `import { CoreError } from '../../shared/errors'; import { completeVerification, createVerification, expireVerification } from './verification.entity'; import type { VerificationRepository } from './verification.repository'; import type { VerificationTokenCodec } from './verification.tokens'; import type { Verification, VerificationCreateInput, VerificationResult } from './verification.types'; export class VerificationService { constructor(private readonly repository: VerificationRepository, private readonly tokens: VerificationTokenCodec, private readonly now: () => Date = () => new Date()) {} async create(input: VerificationCreateInput): Promise<Readonly<{ verification: Verification; token: string }>> { await this.repository.invalidatePending(input.accountId, input.purpose); const token = await this.tokens.issue(); const verification = await this.repository.create(createVerification(input, token.hash, this.now())); return { verification, token: token.token }; } async complete(id: string, token: string): Promise<VerificationResult> { const verification = await this.repository.findById(id); if (!verification || verification.status !== 'pending' || !verification.tokenHash) return { verified: false, status: 'invalidated' }; if (verification.expiresAt <= this.now()) { await this.repository.update(expireVerification(verification, this.now())); return { verified: false, status: 'expired' }; } if (!(await this.tokens.verify(token, verification.tokenHash))) throw new CoreError('verification.invalid_token', 'Verification could not be completed.', {}); const completed = await this.repository.update(completeVerification(verification, this.now())); return { verified: true, status: completed.status }; } }`);
write(file('identity/verification/index.ts'), `export * from './verification.types'; export * from './verification.entity'; export * from './verification.schema'; export * from './verification.repository'; export * from './verification.tokens'; export * from './verification.service';`);

// Authorisation starts here. No authentication provider imports are allowed below.
write(file('roles/system-roles/system-role.types.ts'), `import type { Brand, Timestamped } from '../../shared/primitives'; export type SystemRoleId = Brand<string, 'SystemRoleId'>; export type SystemRoleCode = 'platform_admin' | 'platform_support' | 'platform_finance' | 'platform_moderator' | (string & {}); export type SystemRole = Readonly<{ id: SystemRoleId; code: SystemRoleCode; name: string; status: 'active' | 'inactive'; privileged: boolean } & Timestamped>;`);
write(file('roles/system-roles/system-role.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { SystemRole, SystemRoleCode } from './system-role.types'; export const createSystemRole = (code: SystemRoleCode, name: string, privileged: boolean, now = new Date()): SystemRole => { invariant(name.trim().length > 0, 'system_role.name_required', 'A system role name is required.'); return { id: newId<'SystemRoleId'>(), code, name: name.trim(), privileged, status: 'active', createdAt: now, updatedAt: now }; };`);
write(file('roles/system-roles/system-role.schema.ts'), `import { z } from 'zod'; export const systemRoleSchema = z.object({ code: z.string().regex(/^[a-z][a-z0-9_]*$/), name: z.string().trim().min(1).max(120), privileged: z.boolean() }).strict();`);
write(file('roles/system-roles/system-role.repository.ts'), `import type { SystemRole, SystemRoleCode } from './system-role.types'; export interface SystemRoleRepository { findByCode(code: SystemRoleCode): Promise<SystemRole | null>; save(role: SystemRole): Promise<SystemRole>; }`);
write(file('roles/system-roles/system-role.service.ts'), `import { CoreError } from '../../shared/errors'; import type { SystemRoleRepository } from './system-role.repository'; import type { SystemRole } from './system-role.types'; export class SystemRoleService { constructor(private readonly repository: SystemRoleRepository) {} async assignPrivileged(actorIsPlatformAdmin: boolean, role: SystemRole): Promise<SystemRole> { if (!actorIsPlatformAdmin) throw new CoreError('system_role.assignment_denied', 'This role cannot be assigned through an ordinary workflow.', {}); return this.repository.save(role); } }`);
write(file('roles/system-roles/index.ts'), `export * from './system-role.types'; export * from './system-role.entity'; export * from './system-role.schema'; export * from './system-role.repository'; export * from './system-role.service';`);

write(file('roles/organisation-roles/organisation-role.types.ts'), `import type { Brand, Timestamped } from '../../shared/primitives'; export type OrganisationRoleId = Brand<string, 'OrganisationRoleId'>; export type OrganisationRole = Readonly<{ id: OrganisationRoleId; organisationId: string; code: string; name: string; rank: number; inheritsFrom?: OrganisationRoleId; status: 'active' | 'inactive' } & Timestamped>;`);
write(file('roles/organisation-roles/organisation-role.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { OrganisationRole } from './organisation-role.types'; export const createOrganisationRole = (input: Omit<OrganisationRole, 'id' | 'status' | 'createdAt' | 'updatedAt'>, now = new Date()): OrganisationRole => { invariant(input.rank >= 0, 'organisation_role.invalid_rank', 'Role rank cannot be negative.'); return { ...input, id: newId<'OrganisationRoleId'>(), status: 'active', createdAt: now, updatedAt: now }; };`);
write(file('roles/organisation-roles/organisation-role.schema.ts'), `import { z } from 'zod'; export const organisationRoleSchema = z.object({ organisationId: z.string().uuid(), code: z.string().regex(/^[a-z][a-z0-9_.]*$/), name: z.string().min(1).max(120), rank: z.number().int().min(0), inheritsFrom: z.string().uuid().optional() }).strict();`);
write(file('roles/organisation-roles/organisation-role.repository.ts'), `import type { OrganisationRole } from './organisation-role.types'; export interface OrganisationRoleRepository { findById(id: string): Promise<OrganisationRole | null>; listForOrganisation(organisationId: string): Promise<readonly OrganisationRole[]>; save(role: OrganisationRole): Promise<OrganisationRole>; }`);
write(file('roles/organisation-roles/organisation-role.service.ts'), `import { CoreError } from '../../shared/errors'; import type { OrganisationRoleRepository } from './organisation-role.repository'; import type { OrganisationRole } from './organisation-role.types'; export class OrganisationRoleService { constructor(private readonly repository: OrganisationRoleRepository) {} async assertAssignable(assigner: OrganisationRole, target: OrganisationRole): Promise<void> { if (assigner.organisationId !== target.organisationId || assigner.rank <= target.rank) throw new CoreError('organisation_role.assignment_denied', 'The role cannot be assigned at this scope.', {}); } }`);
write(file('roles/organisation-roles/index.ts'), `export * from './organisation-role.types'; export * from './organisation-role.entity'; export * from './organisation-role.schema'; export * from './organisation-role.repository'; export * from './organisation-role.service';`);

write(file('roles/role-definitions/role-definition.types.ts'), `import type { Brand, Timestamped } from '../../shared/primitives'; export type RoleDefinitionId = Brand<string, 'RoleDefinitionId'>; export type RoleScope = 'platform' | 'organisation' | 'facility' | 'academy' | 'competition'; export type RoleDefinition = Readonly<{ id: RoleDefinitionId; code: string; name: string; scope: RoleScope; description?: string; permissionCodes: readonly string[]; version: number; status: 'active' | 'inactive' } & Timestamped>;`);
write(file('roles/role-definitions/role-definition.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { RoleDefinition } from './role-definition.types'; export const createRoleDefinition = (input: Omit<RoleDefinition, 'id' | 'version' | 'status' | 'createdAt' | 'updatedAt'>, now = new Date()): RoleDefinition => { invariant(input.permissionCodes.every((code) => /^[a-z][a-z0-9_.]*$/.test(code)), 'role_definition.invalid_permission', 'Role definition has an invalid permission code.'); return { ...input, id: newId<'RoleDefinitionId'>(), version: 1, status: 'active', createdAt: now, updatedAt: now }; };`);
write(file('roles/role-definitions/role-definition.schema.ts'), `import { z } from 'zod'; export const roleDefinitionSchema = z.object({ code: z.string().regex(/^[a-z][a-z0-9_.]*$/), name: z.string().min(1).max(120), scope: z.enum(['platform', 'organisation', 'facility', 'academy', 'competition']), description: z.string().max(2000).optional(), permissionCodes: z.array(z.string().regex(/^[a-z][a-z0-9_.]*$/)).max(500) }).strict();`);
write(file('roles/role-definitions/role-definition.repository.ts'), `import type { RoleDefinition } from './role-definition.types'; export interface RoleDefinitionRepository { findByCode(code: string): Promise<RoleDefinition | null>; save(definition: RoleDefinition): Promise<RoleDefinition>; }`);
write(file('roles/role-definitions/role-definition.service.ts'), `import { CoreError } from '../../shared/errors'; import type { RoleDefinitionRepository } from './role-definition.repository'; import type { RoleDefinition } from './role-definition.types'; export class RoleDefinitionService { constructor(private readonly repository: RoleDefinitionRepository) {} async resolveCapabilities(code: string): Promise<readonly string[]> { const definition = await this.repository.findByCode(code); if (!definition || definition.status !== 'active') throw new CoreError('role_definition.unavailable', 'The role definition is not available.', {}); return definition.permissionCodes; } }`);
write(file('roles/role-definitions/index.ts'), `export * from './role-definition.types'; export * from './role-definition.entity'; export * from './role-definition.schema'; export * from './role-definition.repository'; export * from './role-definition.service';`);

write(file('roles/role-assignments/role-assignment.types.ts'), `import type { Brand, Timestamped } from '../../shared/primitives'; import type { RoleDefinitionId, RoleScope } from '../role-definitions'; export type RoleAssignmentId = Brand<string, 'RoleAssignmentId'>; export type RoleAssignmentScope = Readonly<{ type: RoleScope; organisationId?: string; resourceId?: string }>; export type RoleAssignment = Readonly<{ id: RoleAssignmentId; accountId: string; roleDefinitionId: RoleDefinitionId; scope: RoleAssignmentScope; status: 'active' | 'inactive'; startsAt: Date; endsAt?: Date } & Timestamped>; export type RoleAssignmentInput = Readonly<Omit<RoleAssignment, 'id' | 'status' | 'createdAt' | 'updatedAt'>>;`);
write(file('roles/role-assignments/role-assignment.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { RoleAssignment, RoleAssignmentInput } from './role-assignment.types'; export const createRoleAssignment = (input: RoleAssignmentInput, now = new Date()): RoleAssignment => { invariant(!input.endsAt || input.endsAt > input.startsAt, 'role_assignment.invalid_period', 'Assignment end must follow start.'); invariant(input.scope.type === 'platform' || Boolean(input.scope.organisationId), 'role_assignment.missing_scope', 'A non-platform role requires organisation scope.'); return { ...input, id: newId<'RoleAssignmentId'>(), status: 'active', createdAt: now, updatedAt: now }; }; export const isAssignmentEffective = (assignment: RoleAssignment, now = new Date()) => assignment.status === 'active' && assignment.startsAt <= now && (!assignment.endsAt || assignment.endsAt > now);`);
write(file('roles/role-assignments/role-assignment.schema.ts'), `import { z } from 'zod'; export const roleAssignmentSchema = z.object({ accountId: z.string().uuid(), roleDefinitionId: z.string().uuid(), scope: z.object({ type: z.enum(['platform', 'organisation', 'facility', 'academy', 'competition']), organisationId: z.string().uuid().optional(), resourceId: z.string().uuid().optional() }).strict(), startsAt: z.coerce.date(), endsAt: z.coerce.date().optional() }).strict();`);
write(file('roles/role-assignments/role-assignment.repository.ts'), `import type { RoleAssignment } from './role-assignment.types'; export interface RoleAssignmentRepository { save(assignment: RoleAssignment): Promise<RoleAssignment>; listEffective(accountId: string, organisationId?: string): Promise<readonly RoleAssignment[]>; }`);
write(file('roles/role-assignments/role-assignment.service.ts'), `import type { RoleAssignmentRepository } from './role-assignment.repository'; import { createRoleAssignment, isAssignmentEffective } from './role-assignment.entity'; import type { RoleAssignment, RoleAssignmentInput } from './role-assignment.types'; export class RoleAssignmentService { constructor(private readonly repository: RoleAssignmentRepository, private readonly now: () => Date = () => new Date()) {} async assign(input: RoleAssignmentInput): Promise<RoleAssignment> { return this.repository.save(createRoleAssignment(input, this.now())); } async effective(accountId: string, organisationId?: string) { return (await this.repository.listEffective(accountId, organisationId)).filter((assignment) => isAssignmentEffective(assignment, this.now())); } }`);
write(file('roles/role-assignments/index.ts'), `export * from './role-assignment.types'; export * from './role-assignment.entity'; export * from './role-assignment.schema'; export * from './role-assignment.repository'; export * from './role-assignment.service';`);

write(file('permissions/permissions/permission.types.ts'), `import type { Brand, Timestamped } from '../../shared/primitives'; export type PermissionId = Brand<string, 'PermissionId'>; export type PermissionCode = string & { readonly __permissionCode: unique symbol }; export type Permission = Readonly<{ id: PermissionId; code: PermissionCode; description: string; status: 'active' | 'inactive' } & Timestamped>;`);
write(file('permissions/permissions/permission.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { Permission } from './permission.types'; export const createPermission = (code: string, description: string, now = new Date()): Permission => { invariant(/^[a-z][a-z0-9_.]*$/.test(code), 'permission.invalid_code', 'Permission code is invalid.'); return { id: newId<'PermissionId'>(), code: code as Permission['code'], description, status: 'active', createdAt: now, updatedAt: now }; };`);
write(file('permissions/permissions/permission.schema.ts'), `import { z } from 'zod'; export const permissionSchema = z.object({ code: z.string().regex(/^[a-z][a-z0-9_.]*$/), description: z.string().min(1).max(500) }).strict();`);
write(file('permissions/permissions/permission.repository.ts'), `import type { Permission } from './permission.types'; export interface PermissionRepository { findByCode(code: string): Promise<Permission | null>; save(permission: Permission): Promise<Permission>; }`);
write(file('permissions/permissions/permission.service.ts'), `import type { PermissionRepository } from './permission.repository'; import type { Permission } from './permission.types'; export class PermissionService { constructor(private readonly repository: PermissionRepository) {} async resolve(code: string): Promise<Permission | null> { const permission = await this.repository.findByCode(code); return permission?.status === 'active' ? permission : null; } }`);
write(file('permissions/permissions/index.ts'), `export * from './permission.types'; export * from './permission.entity'; export * from './permission.schema'; export * from './permission.repository'; export * from './permission.service';`);

write(file('permissions/permission-groups/permission-group.types.ts'), `import type { Brand, Timestamped } from '../../shared/primitives'; export type PermissionGroupId = Brand<string, 'PermissionGroupId'>; export type PermissionGroup = Readonly<{ id: PermissionGroupId; name: string; permissionCodes: readonly string[]; status: 'active' | 'inactive' } & Timestamped>;`);
write(file('permissions/permission-groups/permission-group.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { PermissionGroup } from './permission-group.types'; export const createPermissionGroup = (name: string, permissionCodes: readonly string[], now = new Date()): PermissionGroup => { invariant(name.trim().length > 0, 'permission_group.name_required', 'A permission group name is required.'); return { id: newId<'PermissionGroupId'>(), name: name.trim(), permissionCodes: [...new Set(permissionCodes)], status: 'active', createdAt: now, updatedAt: now }; };`);
write(file('permissions/permission-groups/permission-group.schema.ts'), `import { z } from 'zod'; export const permissionGroupSchema = z.object({ name: z.string().min(1).max(120), permissionCodes: z.array(z.string().regex(/^[a-z][a-z0-9_.]*$/)).max(500) }).strict();`);
write(file('permissions/permission-groups/permission-group.repository.ts'), `import type { PermissionGroup } from './permission-group.types'; export interface PermissionGroupRepository { findById(id: string): Promise<PermissionGroup | null>; save(group: PermissionGroup): Promise<PermissionGroup>; }`);
write(file('permissions/permission-groups/permission-group.service.ts'), `import type { PermissionGroupRepository } from './permission-group.repository'; import type { PermissionGroup } from './permission-group.types'; export class PermissionGroupService { constructor(private readonly repository: PermissionGroupRepository) {} async resolve(id: string): Promise<PermissionGroup | null> { const group = await this.repository.findById(id); return group?.status === 'active' ? group : null; } }`);
write(file('permissions/permission-groups/index.ts'), `export * from './permission-group.types'; export * from './permission-group.entity'; export * from './permission-group.schema'; export * from './permission-group.repository'; export * from './permission-group.service';`);

write(file('permissions/permission-checks/permission-check.types.ts'), `export type PermissionDecision = Readonly<{ allowed: boolean; reason: 'allowed' | 'unauthenticated' | 'missing_context' | 'not_a_member' | 'missing_permission' | 'policy_denied' | 'indeterminate' }>;
export type PermissionCheck = Readonly<{ actorAccountId?: string; permission: string; organisationId?: string; resource: Readonly<{ type: string; id?: string; ownerAccountId?: string }>; context?: Readonly<Record<string, unknown>> }>;
`);
write(file('permissions/permission-checks/permission-check.schema.ts'), `import { z } from 'zod'; export const permissionCheckSchema = z.object({ actorAccountId: z.string().uuid().optional(), permission: z.string().regex(/^[a-z][a-z0-9_.]*$/), organisationId: z.string().uuid().optional(), resource: z.object({ type: z.string().min(1).max(120), id: z.string().uuid().optional(), ownerAccountId: z.string().uuid().optional() }).strict(), context: z.record(z.unknown()).optional() }).strict();`);
write(file('permissions/permission-checks/permission-check.repository.ts'), `import type { PermissionCheck } from './permission-check.types'; export interface PermissionCheckRepository { isActiveOrganisationMember(accountId: string, organisationId: string): Promise<boolean>; resolvePermissionCodes(accountId: string, organisationId: string | undefined, resource: PermissionCheck['resource']): Promise<readonly string[]>; }`);
write(file('permissions/permission-checks/permission-check.service.ts'), `import type { AccessPolicyService } from '../access-policies'; import type { PermissionCheck, PermissionDecision } from './permission-check.types'; import type { PermissionCheckRepository } from './permission-check.repository'; /** Fail-closed authorization engine. Authentication must already have supplied actor identity. */ export class PermissionCheckService { constructor(private readonly repository: PermissionCheckRepository, private readonly policies: AccessPolicyService) {} async check(request: PermissionCheck): Promise<PermissionDecision> { try { if (!request.actorAccountId) return { allowed: false, reason: 'unauthenticated' }; if (!request.organisationId) return { allowed: false, reason: 'missing_context' }; if (!(await this.repository.isActiveOrganisationMember(request.actorAccountId, request.organisationId))) return { allowed: false, reason: 'not_a_member' }; const permissions = await this.repository.resolvePermissionCodes(request.actorAccountId, request.organisationId, request.resource); if (!permissions.includes(request.permission)) return { allowed: false, reason: 'missing_permission' }; return (await this.policies.evaluate(request)) ? { allowed: true, reason: 'allowed' } : { allowed: false, reason: 'policy_denied' }; } catch { return { allowed: false, reason: 'indeterminate' }; } } }`);
write(file('permissions/permission-checks/index.ts'), `export * from './permission-check.types'; export * from './permission-check.schema'; export * from './permission-check.repository'; export * from './permission-check.service';`);

write(file('permissions/access-policies/access-policy.types.ts'), `import type { Brand, Timestamped } from '../../shared/primitives'; export type AccessPolicyId = Brand<string, 'AccessPolicyId'>; export type AccessPolicy = Readonly<{ id: AccessPolicyId; code: string; description: string; enabled: boolean; organisationId?: string; permission: string } & Timestamped>; export type AccessPolicyContext = Readonly<{ actorAccountId: string; organisationId: string; permission: string; resource: Readonly<{ type: string; id?: string; ownerAccountId?: string }>; facts: Readonly<Record<string, unknown>> }>;`);
write(file('permissions/access-policies/access-policy.entity.ts'), `import { invariant } from '../../shared/errors'; import { newId } from '../../shared/primitives'; import type { AccessPolicy } from './access-policy.types'; export const createAccessPolicy = (input: Omit<AccessPolicy, 'id' | 'enabled' | 'createdAt' | 'updatedAt'>, now = new Date()): AccessPolicy => { invariant(/^[a-z][a-z0-9_.]*$/.test(input.permission), 'access_policy.invalid_permission', 'Policy permission is invalid.'); return { ...input, id: newId<'AccessPolicyId'>(), enabled: true, createdAt: now, updatedAt: now }; };`);
write(file('permissions/access-policies/access-policy.schema.ts'), `import { z } from 'zod'; export const accessPolicySchema = z.object({ code: z.string().regex(/^[a-z][a-z0-9_.]*$/), description: z.string().min(1).max(1000), organisationId: z.string().uuid().optional(), permission: z.string().regex(/^[a-z][a-z0-9_.]*$/) }).strict();`);
write(file('permissions/access-policies/access-policy.repository.ts'), `import type { AccessPolicy } from './access-policy.types'; export interface AccessPolicyRepository { listApplicable(permission: string, organisationId: string): Promise<readonly AccessPolicy[]>; }`);
write(file('permissions/access-policies/access-policy.service.ts'), `import type { PermissionCheck } from '../permission-checks'; import type { AccessPolicyRepository } from './access-policy.repository'; /** Contextual policy evaluation. A policy cannot grant access unless role/permission evaluation already succeeded. */ export class AccessPolicyService { constructor(private readonly repository: AccessPolicyRepository) {} async evaluate(request: PermissionCheck): Promise<boolean> { if (!request.actorAccountId || !request.organisationId) return false; const policies = await this.repository.listApplicable(request.permission, request.organisationId); return policies.every((policy) => policy.enabled); } }`);
write(file('permissions/access-policies/index.ts'), `export * from './access-policy.types'; export * from './access-policy.entity'; export * from './access-policy.schema'; export * from './access-policy.repository'; export * from './access-policy.service';`);
